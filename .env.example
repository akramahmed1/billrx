# Copy to .env and adjust. BillRx never requires AWS to run its test suite or mock demo.
# MODEL_PROVIDER: "bedrock" for live Strands agents, "mock" for the deterministic offline demo.
MODEL_PROVIDER=mock
# Used only when MODEL_PROVIDER=bedrock
MODEL_ID=amazon.nova-lite-v1:0
AWS_REGION=us-east-1
